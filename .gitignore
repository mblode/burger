node_modules
# dist/ is tracked on purpose: it is what npm and CDN consumers load, and the
# source that build:site copies into site/. Ignoring it hides newly generated
# bundles from git status, so the published package silently drifts from src/.
site
.DS_Store
.vercel
.env*
