# Changesets

Run `npm run changeset` to add a changeset when making changes to Burger.

This creates a changeset file that describes the change and its semver bump type (patch, minor, or major). Changesets are consumed during release to update the version and generate changelog entries.
